







































































































































































































































































































































































































































































































































































































































































































































































































#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> n
   
    int t;
    cin >> t;
    

              // number of inputs
    while (t--) {
        int x;
        cin >> x;       // read input
        n.push_back(x); // store it
    }

    for (int v : n) {
        cout << v << " ";
    }
}
