#include<stdio.h>
int main(){
    int b;
    scanf("%d",&b);
    // int a = 10;
    if (b > 8 )
    {
        printf("a is the big");
    }
    else (
        printf("a is smaller")
    );
    return 0;
    
}